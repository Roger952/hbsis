CREATE TABLE instituicao (

            id      BIGINT IDENTITY (1, 1)      NOT NULL,
            name_instituicao    VARCHAR(100)    NOT NULL,
            type_instituicao    VARCHAR(100)    NOT NULL
);